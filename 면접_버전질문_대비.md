# 버전 질문 대비 — Java / Spring Boot / Kotlin

> 현대오토에버 [EnIT] Backend Developer · 인증/계정플랫폼
> 공고 요구: Java, Kotlin, Spring Boot 3.x, JPA/QueryDSL, Redis

---

## 0. 내 실제 상태 (거짓말하지 말 것)

| | 실무 | 개인 프로젝트 |
|---|---|---|
| Java | **11** (부킹엔진), 레거시는 8 | **17** (olive-market) |
| Spring Boot | **2.7** | **3.2** |
| Spring Security | 5.x (CRS 인증) | **6.x** |
| Kotlin | ❌ 없음 | ❌ 학습 중 |

**✅ 결정적 사실**: olive-market의 `SecurityConfig` 는 **Spring Security 6 방식으로 작성되어 있다.**
- `SecurityFilterChain` 을 빈으로 등록 (구버전의 `WebSecurityConfigurerAdapter` 상속 아님)
- `authorizeHttpRequests` 사용 (구버전 `authorizeRequests` 아님)
- `requestMatchers` 사용 (구버전 `antMatchers` 아님)
- 람다 DSL 방식

→ **"Spring Security 6의 변경점을 직접 겪었다"** 고 말할 근거가 실제로 있다.

---

## 1. "Spring Boot 3.x 경험 있으신가요?"

### 답변

> "실무에서는 Spring Boot **2.7**까지 사용했고, **3.2는 개인 프로젝트로 다뤘습니다.**
>
> 3.x로 오면서 가장 크게 체감한 건 두 가지였습니다.
> 첫째는 **`javax` 패키지가 `jakarta` 로 전부 바뀐 것**입니다. 이건 코드 한 줄씩 다 고쳐야 하는 부분이라, 기존 프로젝트를 올린다면 여기가 가장 손이 많이 갈 지점이라고 봤습니다.
>
> 둘째는 **Spring Security 6의 설정 방식 변경**입니다. 기존에는 `WebSecurityConfigurerAdapter` 를 상속받아 `configure()` 를 오버라이드했는데, 6부터는 그 클래스가 없어지고 **`SecurityFilterChain` 을 빈으로 등록하는 방식**으로 바뀌었습니다. `authorizeRequests` → `authorizeHttpRequests`, `antMatchers` → `requestMatchers` 처럼 메서드명도 함께 정리됐고요.
>
> 개인 프로젝트에서 JWT 인증 필터를 붙이면서 이 새 방식으로 직접 구성해봤습니다."

**⚠️ 실무 2.7이라는 사실을 먼저 밝힐 것.** 숨기고 시작하면 뒤에서 무너진다.

---

## 2. "Java 21은 써보셨나요?"

### 답변

> "실무는 **Java 11**, 개인 프로젝트는 **17**까지 사용했고 **21은 아직 실무로 다뤄보지 못했습니다.**
>
> 다만 21에서 가장 관심 있게 본 건 **Virtual Thread** 입니다. 기존에는 요청 하나에 OS 스레드 하나가 묶여서, 동시 요청이 늘면 스레드 풀이 병목이 됐습니다. Virtual Thread는 JVM이 관리하는 가벼운 스레드라 **I/O 대기 중에 캐리어 스레드를 반납**하기 때문에, 적은 리소스로 많은 동시 요청을 처리할 수 있는 것으로 이해하고 있습니다.
>
> 인증 플랫폼처럼 **짧고 I/O 위주인 요청이 대량으로 들어오는 환경**에서는 특히 효과가 클 것 같아, 이 부분은 꼭 경험해보고 싶습니다."

**포인트**: 모른다고 끝내지 말고 **"왜 그 기능이 이 직무와 관련 있는지"** 까지 연결하면 학습 태도가 보인다.

---

## 3. "Kotlin 경험은요?"

### 답변

> "**실무 경험은 없습니다.** 현재 학습 중이고, 개인 프로젝트는 Java 17로 진행하고 있습니다.
>
> Kotlin을 보면서 인상적이었던 건 **null 안전성이 타입 시스템에 들어가 있다**는 점입니다. Java에서는 NPE를 런타임에 만나지만 Kotlin은 컴파일 시점에 걸러지고요. `data class` 로 DTO 보일러플레이트가 줄어드는 것도 실무에서 체감이 클 것 같았습니다.
>
> Java와 상호 운용이 되니 기존 코드와 섞어 쓰면서 점진적으로 전환하는 방식이 현실적이라고 보고 있습니다."

**⚠️ "할 수 있습니다" 같은 막연한 자신감 금지.** 모르는 건 모른다고 하되, **뭘 알아봤는지**는 말할 수 있어야 한다.

---

## 4. 🔥 "레거시를 최신 버전으로 올려본 경험이 있나요?"

### 이 질문이 나오면 최대 기회다

> "네, **Spring Framework 레거시 버전을 최신으로 업그레이드하는 작업**을 직접 수행했습니다.
>
> 함께 **iBatis를 MyBatis로 마이그레이션**했는데, 단순히 라이브러리를 교체하는 게 아니라 **기존 쿼리 XML 구조와 동작을 하나씩 검증하면서 옮겨야** 했습니다. 운영 중인 솔루션이라 기능이 깨지면 안 됐기 때문에, 변경 범위를 나눠서 단계적으로 적용했습니다.
>
> 이 경험에서 배운 건 **버전 업그레이드는 기술 문제라기보다 '무엇이 깨질 수 있는지 미리 아는 것'의 문제**라는 점이었습니다. 3.x 전환도 `javax`→`jakarta` 처럼 범위가 명확한 부분과, 의존 라이브러리 호환성처럼 예측이 어려운 부분을 나눠서 접근해야 한다고 생각합니다."

**이게 네가 가진 가장 강한 카드다.** 최신 버전을 써본 사람은 많아도, **버전을 올려본 사람은 드물다.**

---

## 5. 최소한 알고 갈 버전 차이

### Java

| 버전 | 핵심 |
|---|---|
| **11** (LTS) | `var`, 표준 `HttpClient`, `String.isBlank/lines/strip` |
| **17** (LTS) | **Record**, Sealed Class, `instanceof` 패턴 매칭, Text Block |
| **21** (LTS) | **Virtual Thread**, switch 패턴 매칭, Record Pattern, Sequenced Collections |

### Spring Boot

| 버전 | 핵심 |
|---|---|
| **2.7** | Java 8 이상, `javax` 패키지 |
| **3.0** | **Java 17 최소**, **`javax` → `jakarta`**, Spring Framework 6, GraalVM Native Image |
| **3.2** | **Virtual Thread 지원** (`spring.threads.virtual.enabled=true`), JDK 21 지원 |

### Spring Security 5 → 6 (인증 직무라 특히 중요)

| 구분 | 5.x | **6.x** |
|---|---|---|
| 설정 방식 | `WebSecurityConfigurerAdapter` 상속 | **`SecurityFilterChain` 빈 등록** |
| 인가 설정 | `authorizeRequests()` | **`authorizeHttpRequests()`** |
| 경로 매칭 | `antMatchers()` / `mvcMatchers()` | **`requestMatchers()` 로 통합** |
| 스타일 | 체이닝 | **람다 DSL** |

→ **이 표는 외워갈 것.** olive-market 코드가 전부 오른쪽 방식이므로, 코드를 보여주며 설명할 수도 있다.

---

## 6. 절대 하지 말 것

- ❌ "Java 21 써봤습니다" (안 써봤음)
- ❌ "Kotlin 할 수 있습니다" (경험 없음)
- ❌ Virtual Thread를 깊게 아는 척 (개념까지만)
- ❌ 실무 2.7을 숨기고 3.x 위주로 말하기

**모른다 → 대신 무엇을 알아봤다 → 왜 관심 있다** 순서면 감점이 아니라 가점이다.
